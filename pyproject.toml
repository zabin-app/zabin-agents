[project]
name = "portable-agent-contracts"
version = "0.1.0"
description = "Versioned contracts for portable agents and Zabin MCP policy"
requires-python = ">=3.11"
dependencies = []

[tool.contracts]
python = "python >=3.11"
test = "python -m unittest tests.test_contract_schemas"
test_discovery = "python -m unittest discover -s tests -p 'test_*.py'"
